# Configuration SMTP pour l'envoi d'emails
# Ces paramètres sont nécessaires pour que le formulaire de contact fonctionne

# Serveur SMTP (exemples: smtp.gmail.com, smtp.office365.com, smtp.mailgun.org)
SMTP_HOST=smtp.gmail.com

# Port SMTP (587 pour TLS, 465 pour SSL)
SMTP_PORT=587

# Utiliser SSL/TLS (true pour port 465, false pour port 587)
SMTP_SECURE=false

# Identifiants SMTP
# Pour Gmail: utilisez un "App Password" (Mot de passe d'application)
# Générez-le dans: https://myaccount.google.com/apppasswords
SMTP_USER=votre-email@gmail.com
SMTP_PASS=votre-mot-de-passe-application

# Adresse email d'envoi (optionnel, utilise SMTP_USER par défaut)
SMTP_FROM=noreply@fittrack.app

# Email de l'administrateur (reçoit les messages du formulaire de contact)
ADMIN_EMAIL=admin@votredomaine.com

# ============================================
# Configuration Gmail - Guide rapide
# ============================================
# 1. Activez la validation en deux étapes sur votre compte Google
# 2. Allez sur: https://myaccount.google.com/apppasswords
# 3. Créez un nouveau mot de passe d'application pour "Mail"
# 4. Utilisez ce mot de passe dans SMTP_PASS
#
# ⚠️ IMPORTANT: N'utilisez JAMAIS votre mot de passe Gmail normal
# ⚠️ Ne committez JAMAIS ce fichier .env dans Git

# ============================================
# Autres fournisseurs SMTP populaires
# ============================================
# 
# Office365/Outlook:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# 
# SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=votre-clé-api-sendgrid
# 
# Mailgun:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@votre-domaine.mailgun.org
# SMTP_PASS=votre-mot-de-passe-mailgun

# URL du frontend (pour les liens dans les emails)
FRONTEND_URL=http://localhost:8080

# JWT Secret pour l'authentification (changez-le en production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
